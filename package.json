{
    "name": "@simplisticated/zip-monster",
    "version": "1.3.180",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "prepare": "npm run build",
        "build": "rm -rf dist && tsc",
        "test": "rm -rf dist && jest --passWithNoTests"
    },
    "dependencies": {
        "geolib": "^3.3.3"
    },
    "devDependencies": {
        "@types/jest": "27.0.2",
        "@types/node": "17.0.3",
        "jest": "27.3.0",
        "ts-jest": "27.0.7",
        "typescript": "4.1.2"
    }
}
